# Agent instructions (SoleSync Sells)

**What this repo is:** Reselling inventory management — online marketplace sales (e.g. eBay, Depop) plus wholesale; Next.js, MongoDB, next-auth.

**Where rules live:** Persistent, scoped agent instructions are in [`.cursor/rules/`](.cursor/rules/) (e.g. product context, keeping `database-examples/` in sync with schema changes). Prefer updating those for anything that should apply on every Agent chat.
